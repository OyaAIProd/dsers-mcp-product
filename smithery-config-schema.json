{
  "type": "object",
  "required": ["dsers_session_id", "dsers_state"],
  "properties": {
    "dsers_session_id": {
      "type": "string",
      "title": "DSers Session ID",
      "description": "DSers session ID. Run 'npx @lofder/dsers-mcp-product login' to get it.",
      "x-order": 0
    },
    "dsers_state": {
      "type": "string",
      "title": "DSers State",
      "description": "DSers state token (obtained together with session ID)",
      "x-order": 1
    },
    "default_country": {
      "type": "string",
      "title": "Default Country",
      "description": "Default target country code for shipping and pricing (e.g. US, GB, DE). Applied when country is not specified per-import.",
      "default": "US",
      "x-order": 3,
      "x-from": { "header": "x-dsers-default-country" }
    },
    "default_store": {
      "type": "string",
      "title": "Default Store",
      "description": "Default target store name. Skips store selection when pushing to one store. Use the display_name from dsers_store_discover.",
      "x-order": 4,
      "x-from": { "header": "x-dsers-default-store" }
    },
    "default_visibility": {
      "type": "string",
      "title": "Default Visibility",
      "description": "Default product visibility: backend_only (draft) or sell_immediately (published)",
      "default": "backend_only",
      "enum": ["backend_only", "sell_immediately"],
      "x-order": 5,
      "x-from": { "header": "x-dsers-default-visibility" }
    },
    "price_multiplier": {
      "type": "number",
      "title": "Default Price Multiplier",
      "description": "Default pricing multiplier for all imports when no per-import pricing rule is set (e.g. 2.0 for 2x markup)",
      "x-order": 6,
      "x-from": { "header": "x-dsers-price-multiplier" }
    }
  }
}
